# lets ignore spelling mistakes for now, there are too many
spelling-error-in-binary

# we *want* to have a custom RPATH for private shared libraries, which is
# a valid use described in the lintian tag message.
custom-library-search-path RUNPATH /usr/lib/aarch64-linux-gnu/camx/kodiak [*]
custom-library-search-path RUNPATH /usr/lib/aarch64-linux-gnu/camx/kodiak/hw [*]

# libshdr3 still carries the legacy non-multi-arch private library path.
custom-library-search-path RUNPATH /usr/lib/camx/kodiak [*]

# upstream does not compile with bindnow
hardening-no-bindnow [*]
